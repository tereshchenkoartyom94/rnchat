import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  container: {
    paddingBottom: 50,
    flex: 1,
  },
  separator: {
    marginBottom: 7,
    flexDirection: 'row',
    flex: 1,
    justifyContent: 'flex-end',
  }
});
